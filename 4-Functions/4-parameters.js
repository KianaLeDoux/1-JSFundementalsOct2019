// Parameters


function hi(name) {
    // 1.
    console.log(`Hi ${name}!`); // another form of string interpolation
}                     //2. 

hi('Kiana'); 
//3.
hi('Danielle');
hi('Eli');
hi('John');

// 1- the paarmeters we need to take into the function
//2. - using string interpolation, we can refer to the parameter we passed
// 3. - we define what the parameter's vakye will be.

// function pet(animal) {
//     console.log(`My pet's name is ${animal}!`);
// }

// pet('Louie');

// function firstName(Kiana) + lastName (LeDoux) {
// console.log(`Hello, my name is ${firstName +lastName});
// }


function firstName, lastName(Kiana LeDoux) {
 let first, last;
 first = document.getElementById ("firstName").nodeValue;
 second = document.getElementById ("firstName").nodeValue;
}

function introduction (fName, lName) {
    let fullName = fName +' ' + lName;
    console.log(`Hello, my name is ${fullName}`);
}

introduction ('Kiana', 'LeDoux');