// Assignment Operator 
let r = 5; // the equal sign is assigning a value to the variable name

// Addition assignment 
let newR = r + 1; // can also rewrite it the way it is below
r += 1;
console.log(r+=1);

//* Subtraction assignment 
r -= 1; // similar to if you did r = r = 1;

r *= 1; // multiplication assignment //r=r *1;

r /= 1; // r =r / 1;

//* Remainder/Modulus assignment 
r %= 1; // r = r % 1;

// Exponential assignment 
r **=1; // r=r ** 1;